---
title: "Joining & Verifying the Client — Domain Join Validation"
sidebarTitle: "Validation"
description: "Placing the Windows 11 client on the isolated network, joining it to the domain, and verifying the lease, gateway, and domain login all work end to end."
icon: "circle-check"
---

This lab is a work in progress rather than a finished build. This page tracks what's actually been verified against what's still planned, updated as each phase lands.

## Confirmed Working

| Item | Verification |
| --- | --- |
| Domain controller promoted | `dcdiag` and `repadmin /replsummary` run clean (aside from a benign `SystemLog` false-positive — see [Build](/projects/ad-homelab/build)) |
| Host-only network isolation | `VMnet1` static addressing confirmed, DHCP disabled, no cross-talk with the home network |
| OU tree and department groups | 8 departments, each with `Users`/`Groups`/`Computers` sub-OUs and a `-Staff` group, built via `setup-ou-structure.ps1` |
| Bulk user provisioning | 1,050 accounts created, 0 skipped, verified via `Get-ADUser -Filter * -SearchBase 'OU=Corp,DC=corp,DC=lab' \| Measure-Object` |
| Help-desk delegation | `Reset user passwords` permission delegated to the `Helpdesk` group on the `IT` OU |

## In Progress / Queued

<Steps>
  <Step title="GPOs">
    At least 2–3 Group Policy Objects that do something observable — a password policy, a desktop restriction, a mapped drive — applied and confirmed on a domain-joined machine.
  </Step>
  <Step title="Client join">
    A Windows 11 client (`CLIENT01`) built on `VMnet1` with a static IP, joined to `corp.lab`, and used to confirm GPOs actually apply (`gpupdate /force`).
  </Step>
  <Step title="Security telemetry">
    Sysmon deployed to DC01 and the client, forwarding to a log server (Splunk or Wazuh) for the blue-team half of the lab.
  </Step>
  <Step title="Attacker tooling">
    The existing Kali VM wired onto `VMnet1` with a second (host-only) network adapter, BloodHound collection run against the domain, and a first attack/detection scenario (Kerberoasting or password spray) run and documented end to end.
  </Step>
</Steps>

## Why This Page Reads Like a Checklist

A completed writeup with a tidy "everything worked" narrative is less useful than an honest record of what's actually been done — this page exists so the state of the lab is always clear at a glance, rather than implying validation that hasn't happened yet. Each item above moves to "Confirmed Working" once it's actually been run and checked, not before.